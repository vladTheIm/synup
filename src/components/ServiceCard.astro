---
export interface Props {
  title: string;
  description: string;
  icon: string;
}

const { title, description, icon } = Astro.props;
---

<div class="service-card">
  <div class="service-icon" set:html={icon}></div>
  <h3>{title}</h3>
  <p>{description}</p>
</div>
